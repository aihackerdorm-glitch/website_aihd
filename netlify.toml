[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  NPM_FLAGS = "--legacy-peer-deps"

# Ensure SPA routing works with react-router-dom
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
